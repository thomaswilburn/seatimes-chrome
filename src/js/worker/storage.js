/*

This module handles storing and retrieving specific datatypes in IndexedDB.

Almost everything in the app gets stored in a single content database. There are a few exceptions.

*/

var Database = require("./idb");

